
<div class="container">
<div class="" id="spati-map">
  <h1> Here are the Spätis </h1>
  <div class="row">
    <div class="col-6 spati-list">
      <ul>
      <% @spatis.each do |spati| %>
      <li><h3><%= link_to spati.name, spati_path(spati) %></h3>
      <%= spati.address %><br>
      <%= spati.opening_time %></li>
      <% end %>
      </ul>
      </div>
      <div class="col-6 map-container">
        <div id="map" style="width: 100%; height: 800px;"
          data-controller="map"
          data-map-markers-value="<%= @markers.to_json %>"
          data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
  </div>
</div>
<</div>
