<h2>Edit Your Story</h2>

<%= simple_form_for(@story) do |form| %>
  <div>
    <%= form.input :content %>
  </div>

  <div>
    <%= form.submit "Update Story" %>
  </div>
<% end %>

<%= link_to "Back", spati_path(@spati) %>
